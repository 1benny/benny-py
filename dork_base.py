import requests
import os
import sys
import subprocess

class dork:
    def __init__(self, query, format):
        self.query = query
        self.format = format

    def search():
        dork_get = 
