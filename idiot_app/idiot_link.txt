from pytube import YouTube
import os

url = "https://youtu.be/48rz8udZBmQ"

video = YouTube(url)

dl = video.streams.filter(only_audio=True).first()

dest = "C:\\Users\\Ben\\Desktop\\"

out_file = dl.download(output_path=dest)

base, ext = os.path.splitext(out_file)
new_file = base + ".mp3"

print(dl.title)
print("Done...")

